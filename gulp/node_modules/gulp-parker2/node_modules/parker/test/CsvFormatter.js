/*! Parker v0.0.0 - MIT license */

/*var expect = require('chai').expect,
    CsvFormatter = require('../lib/CsvFormatter.js');

describe('The CSV Formatter', function () {
	it('should return data in CSV format', function () {
		var mockData = {'mock-metric-1': 3, 'mock-metric-2': 4},
			mockMetrics = [{id: 'mock-metric-1', name: 'Mock Metric 1'}, {id: 'mock-metric-2', name: 'Mock Metric 2'}]
			csvFormatter = new CsvFormatter(),
			output = csvFormatter.format(mockData, mockMetrics);

		var expectedOutput = "\"Mock Metric 1\",\"Mock Metric 2\"\n"
			+ "\"3\",\"4\"\n";

		expect(output).to.equal(expectedOutput);

	});

	it('should ')
});*/